import { it, expect, describe } from 'vitest';
import {  screen } from "@testing-library/react";

describe('group', () => {
  it('should', () => {
    screen.debug()
    expect(5).toBeTruthy();
  })
})